Q1 Conditional Statements
Input: one integer n.
Reads n and prints “negative one” (-1), “zero” (0), “positive one” (1), or “other value”.

Q2 Printing a Vector
Input: a vector<int> v (given in code).
Prints all numbers in v in a simple format like [1, 2, 3].

Q3 While Loops
Input: none (fixed limit = 4,000,000).
Prints Fibonacci numbers starting from 1 and 2, and stops when the next number is too big.

Q4 Functions
4.1 If Prime
4.2 Factorize
4.3 Prime Factorization
Input: an integer n (passed into functions in tests).
Checks if n is prime, prints all factors of n, and prints the prime factors of n.

Q5 Recursive Functions and Loops
Input: one integer n (number of rows).
Prints the first n rows of Pascal’s Triangle, one row per line.
